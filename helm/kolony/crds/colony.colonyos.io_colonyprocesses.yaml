---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: colonyprocesses.colony.colonyos.io
spec:
  group: colony.colonyos.io
  names:
    kind: ColonyProcess
    listKind: ColonyProcessList
    plural: colonyprocesses
    singular: colonyprocess
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.funcName
      name: Function
      type: string
    - jsonPath: .spec.executorType
      name: Executor
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.assignedExecutor
      name: Assigned
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: ColonyProcess is the Schema for the colonyprocesses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ColonyProcessSpec defines the desired state of ColonyProcess
            properties:
              args:
                description: Args are positional arguments passed to the function
                items:
                  type: string
                type: array
              conditions:
                description: Conditions specify requirements for executor selection
                properties:
                  cpu:
                    description: CPU is the number of CPU cores required
                    type: integer
                  gpu:
                    description: GPU specifies GPU requirements
                    properties:
                      count:
                        description: Count is the number of GPUs required
                        type: integer
                      memory:
                        description: Memory is the GPU memory required in MB
                        type: integer
                      name:
                        description: Name is the GPU model name (e.g., "nvidia-a100")
                        type: string
                    type: object
                  memory:
                    description: Memory is the amount of memory required in MB
                    type: integer
                  nodes:
                    description: Nodes is the number of nodes required
                    type: integer
                type: object
              env:
                additionalProperties:
                  type: string
                description: Env are environment variables to set for the process
                type: object
              executorType:
                description: ExecutorType is the type of executor to run this process
                type: string
              funcName:
                description: FuncName is the name of the function to execute
                type: string
              kwargs:
                description: Kwargs are keyword arguments passed to the function
                type: object
                x-kubernetes-preserve-unknown-fields: true
              maxExecTime:
                default: 3600
                description: MaxExecTime is the maximum execution time in seconds
                type: integer
              maxRetries:
                default: 0
                description: MaxRetries is the maximum number of retry attempts
                type: integer
              maxWaitTime:
                default: 600
                description: MaxWaitTime is the maximum time in seconds to wait in
                  queue
                type: integer
            required:
            - executorType
            - funcName
            type: object
          status:
            description: ColonyProcessStatus defines the observed state of ColonyProcess
            properties:
              assignedExecutor:
                description: AssignedExecutor is the name of the executor running
                  this process
                type: string
              completionTime:
                description: CompletionTime is when the process completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the ColonyProcess
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              errors:
                description: Errors contains any error messages
                items:
                  type: string
                type: array
              output:
                description: Output contains the process output
                items:
                  type: string
                type: array
              processId:
                description: ProcessID is the ID of the process in ColonyOS
                type: string
              retries:
                description: Retries is the number of retry attempts made
                type: integer
              startTime:
                description: StartTime is when the process started executing
                format: date-time
                type: string
              state:
                description: State is the current state of the process
                enum:
                - Pending
                - Waiting
                - Running
                - Success
                - Failed
                type: string
              submitTime:
                description: SubmitTime is when the process was submitted to ColonyOS
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
